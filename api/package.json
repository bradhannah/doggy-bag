{
  "name": "budgetforfun-api",
  "version": "0.6.1",
  "description": "Bun HTTP backend for doggy-bag",
  "type": "module",
  "scripts": {
    "server": "bun run server.ts",
    "spec": "tsoa spec",
    "spec:watch": "tsoa spec --watch"
  },
  "dependencies": {
    "@tsoa/runtime": "^7.0.0-alpha.0",
    "tsoa": "^7.0.0-alpha.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
